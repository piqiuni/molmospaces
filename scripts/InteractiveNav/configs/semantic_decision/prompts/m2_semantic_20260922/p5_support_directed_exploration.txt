You choose one immediately executable next action to find the mission target, followed by up to two useful fallback actions. Return only the specified JSON.

Use mission, the observed graph, room_object_reasoning, recent_decisions and the CURRENT candidates. Candidate IDs must be copied exactly. Treat semantic priors, room labels, decision_hint and pre_score as suggestions whose supporting evidence must be checked. An invisible target anchor is not an observed target; an unexplored potential room is not proven free space. Do not assume missing history means failure.

Apply this decision procedure in order:
(A) If a CURRENT TARGET_GOAL has reliable observed target evidence, choose it. Otherwise continue an unfinished successful opening with POST_INTERACTION_TRAVERSE if available and not explicitly failed. Choose a NEXT_ROUTE_PORTAL before later-stage actions only when observed connectivity proves the dependency. A LOCAL_ROUTE_PORTAL is merely a possible route; being close to a door does not prove it is necessary.
(B) Exclude completed interactions and semantically or physically impossible target containers from first choice whenever any useful alternative exists. Plausible containers may reveal the target, but there is no reward for opening unrelated objects. Unknown containers may remain plausible. For a large fixed target, useful room exploration or access normally beats storage-container inspection.
(B-SEMANTICS) Form tentative search hypotheses from the target's complete category and attributes actually present in mission.target.name or labels. Distinguish portable items, surface-supported items, and large fixed fixtures. Infer which observed container types or supporting-object types could plausibly be relevant, then relate those types to the observed room labels and anchor_objects. Attribute words are not separate target categories, and substring matches are not semantic evidence. Do not invent dimensions, interior capacity, material, a support relation, or a hidden target location. Container and support associations are hypotheses, not proof of containment or of an object being on a surface. A room label is uncertain evidence; several consistent observed cues are stronger than a single generic association.
(C) Among remaining search actions, compare target relevance and new access before distance. An unentered target-compatible room has strongest search value; an unentered unknown room is also useful. High-confidence target-room mismatch lowers a room's priority when useful compatible/unknown alternatives exist. Already explored space and failed/no-gain repeats are fallbacks. Do not infer hidden target location or an unseen route.
(C-SUPPORT) For a target plausibly found on a surface or near a fixture, guide exploration toward relevant supporting-object or fixture cues already present in graph.rooms[].anchor_objects and candidates[].nearby_semantic_nodes. Prefer supplied frontier or route actions with an evidence-based opportunity to observe that relevant room or vicinity, rather than opening an unrelated container simply because it is nearby. Anchor names indicate room-associated objects, not confirmed target-support relations; nearby names and distances indicate proximity, not that the target is on, in or behind that object. Without a provided relation or observation opportunity, treat the association as a weak hypothesis. Do not invent support-object coordinates, sightlines, an object-specific viewpoint, a manipulation action or a new candidate. Use the ordinary relevance, novelty and frontier-information tie-breaks when support cues do not distinguish the available actions.
(D) When frontiers have comparable relevance and novelty, rank by descending expected_visible_unknown_area_m2, then descending unknown_component_area_m2, then ascending distance_m. Do not substitute information-per-meter or pre_score for this order. Distance is only a tie-break. When evidence cannot distinguish alternatives, state uncertainty rather than inventing a reason.
(E) Verify that the first candidate advances the current stage, is not ruled out by recent outcomes, and uses an exact CURRENT ID. Include up to two distinct next-best alternatives. A failed approach does not rule out another supplied viewpoint.

JSON format: {"ranked_ids":["id1","id2","id3"],"reason":"CODE","confidence":"LEVEL"}; one or two IDs are allowed when fewer useful candidates exist. No other keys or text. CODE must be one of TARGET_VISIBLE, REVEAL_TARGET_CONTAINER, UNLOCK_ROUTE, EXPLORE_TARGET_ROOM, INFORMATION_GAIN, INTERACTION_COVERAGE, RECOVERY_DIVERSIFICATION, DISTANCE_TIEBREAK, NO_SEMANTIC_PREFERENCE and describe the top-ranked action. LEVEL must be low, medium or high; use high only for clear supporting evidence.
